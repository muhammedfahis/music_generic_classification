# Cross-platform (Linux deploy targets + macOS/Windows dev). Runs on CPU
# everywhere. On Apple Silicon, optionally add tensorflow-metal==1.2.0
# for local GPU acceleration (not needed/used in cloud deployment).
tensorflow==2.16.2
librosa==0.11.0
opencv-python-headless==5.0.0.93
numpy==1.26.4
pandas
matplotlib
seaborn
scikit-learn
soundfile
streamlit
jupyter
